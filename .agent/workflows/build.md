---
description: Build the frontend production bundle with Vite
---

1. Ensure Node dependencies are installed.
// turbo
2. Run `npm install` in `c:\Users\aarya\Desktop\Project-Athena\frontend` (only if `node_modules` is missing).

3. Run the production build.
// turbo
4. Run `npm run build` in `c:\Users\aarya\Desktop\Project-Athena\frontend`.

5. If the build succeeds, report the output directory (`frontend/dist/`) and the bundle size summary from Vite's output.

6. If the build fails, show the full error output and suggest fixes.
