---
description: Stage, commit, and push all changes to GitHub
---

1. Check the current git status to see what has changed.
// turbo
2. Run `git status` in `c:\Users\aarya\Desktop\Project-Athena` to see all modified/untracked files.

3. Stage all changes.
// turbo
4. Run `git add .` in `c:\Users\aarya\Desktop\Project-Athena`.

5. Ask the user for a commit message if they haven't provided one. If they have, use it directly.

6. Commit with the provided or generated message.
// turbo
7. Run `git commit -m "<message>"` in `c:\Users\aarya\Desktop\Project-Athena`.

8. Push to the remote origin.
// turbo
9. Run `git push origin master` in `c:\Users\aarya\Desktop\Project-Athena`.

10. Report the push result to the user, including the branch and commit SHA.
